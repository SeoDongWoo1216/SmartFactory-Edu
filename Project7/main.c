#include <stdio.h>

int main(void)
{
	//int sub;
	//int a, b;
	//printf("정수 두 개를 입력하시오 : ");
	//scanf_s("%d %d", &a, &b);

	//sub = (a > b) ? (a - b) : (b - a);
	//printf(" 두 정수의 차는 %d 입니다.\n", sub);

	////////////////////////////////////////////

	//unsigned char A = -1;  // unsigned는 음수를 취급하지 않는다.
	//unsigned int B = A;    // 그래도 -1을 넣으면 제일 큰 수가 들어간다.
	//printf("A = %d\n", (signed char)A);
	//printf("B = %d\n", B);
	//return 0;

	////////////////////////////////////////////

	/*char a = 'a', m = 32;
	a = a ^ m;
	printf("a의 값 : %c \n", a);
	a = a ^ m;
	printf("a의 값 : %c \n", a);
	return 0;*/

	//////////////////////////////////////////

	/*int a = 35;
	printf("a에 대한 1의 보수 값 : %u \n", ~a);
	printf("a에 대한 2의 보수 값 : %d \n", ~a + 1);
	return 0;*/

	//////////////////////////////////////////

	/*int a = 0x4;
	int b = 0x80000004;
	unsigned int c = 0x80000004;
	printf("a = %d, b = %d, c = %u \n", a, b, c);
	printf("a << 2 => %d \n", a << 2);
	printf("a >> 2 => %d \n", a >> 2);
	printf("a >> 3 => %d \n", a >> 3);
	printf("b << 1 => %d \n", a << 1);
	printf("b >> 1 => %d \n", a >> 1);
	printf("c >> 1 => %d \n", a >> 1);*/
}











//int main2() {
	/*int n = 24.5;
	double d = 3;
	char c = 129;

	printf("%d, %f, %c \n", n, d, c);
	return 0;*/

	//////////////////////////////////////////////////////

	//double time = 0.375;
	//int min;
	//printf("0.375는 몇 분, 몇 초 인가? \n");
	//time = time * 60;   // 분 단위로 환산
	//min = (int)time;    // 정수값만 추려서 min에 저장
	//time = time - min;  // time에는 분 단위 값만 남는다.

	//printf("분 : %d \n", min);
	//printf("초 : %d \n", (int)(time * 60));

	/////////////////////////////////////////////////////////
	
	/*char a = 'a';
	char b = 'b';
	char m = (char)0xdf;
	char n = 1;

	printf("a & m : %o \n", a & m);
	printf("b의 첫번째 비트 : %d \n", b&n);
	return 0;*/

	///////////////////////////////////////////////////////////

	//int a = 15;
	//int b = 20;
	//int c = a | b;
	//printf("OR 연산 결과 : %d\n", c);
	//return 0;

	/////////////////////////////////////////////////////////////
//}

